2026-07-16
- Simplified Orders service badges: hide skipped traces and show compact format (service · qty · panel).
- Updated duplicate handling message to exactly "Duplicate order" and keep duplicate attempts as failed.
- Prepared release v1.3.6 and push/deploy pipeline.

2026-07-17
- Replaced tier-based service mapping with slot-based routing and free-text keyword matching.
- Added deterministic shuffle for duplicate keyword-matched slots and default-slot behavior for unmatched links.
- Added Services "Sync slots" action and backend slot normalization endpoint for contiguous slot numbering.
- Updated keyword input UX: comma is separator, Enter commits split keywords.
- Added provider-agnostic schema migration `supabase/migrations/0001_provider_agnostic_slots.sql` and updated setup docs.
- Added comment pool delete support in Comments UI and API.
- Added localhost-safe fallback for missing optional tables (`watched_x_accounts`/`orders`) during pool deletion.
- Made X comment-pool category automatic: removed manual category input from upload UI and infer category from configured X comment-slot keywords.
- Switched background order submission from sequential loop to concurrent batch execution using Promise.allSettled.

2026-07-20
- Added global keyword-aware fallback routing for failed/canceled services using new `service_presets.is_fallback` slots.
- Updated Services UI/API to manage fallback slots and preserve existing keyword matching behavior.
- Updated orders sync/manual retry flows to auto-retry failed services using fallback slots with different service IDs.
- Added Supabase 10-minute scheduler support via `pg_cron` + `pg_net` with secure endpoint `/api/cron/sync-orders`.
- Added migrations `0002_service_preset_fallback.sql` and `0003_supabase_sync_cron.sql`.
- Updated fallback behavior so blank fallback qty/keywords inherit from failed service context during retry.
- Grayed out and disabled fallback qty/keyword inputs in Services when fallback is enabled.
- Fixed Orders failed-tab visibility by moving status filtering server-side (not limited to cached latest 25 rows).
- Added Orders previous/next pagination so older completed/failed history is browsable.
- Fixed pagination regression where clicking Next returned to page 1 by removing page-coupled effect triggers.
- Updated slot routing so duplicate no-keyword presets also shuffle deterministically per link, keeping exactly one selected service per link/category.
- Added slot-selection debug output to order results and surfaced it in the Orders page for quick verification.
- Prepared release v1.3.14 and push/deploy pipeline.

2026-08-07
- Updated Orders retry behavior to sync-first: clicking Retry now syncs provider state for the target order before resubmission.
- Retry now resubmits only service types that failed due to low provider balance/funds, instead of forcing fallback for all failed services.
- Added retry response details for UI feedback (`retry=false` no-op case and `retriedServiceTypes` list), and updated retry button/feedback copy in Orders page.
- Fixed fallback inheritance when fallback slots have blank quantity/keywords by inheriting from matching primary-slot selection for the same link/service type, preventing zero-quantity failures ("Quantity must be greater than 0").
- No new SQL migration.
- Added one-click global retry flow for low-balance failures: new Orders action retries all eligible low-balance failed services across recent orders after sync-first provider refresh.
- Added top-level "Retry all low balance" button in Orders UI with summary feedback (scanned/eligible/retried/failed counts) and disabled per-order retry while bulk retry runs.
- No new SQL migration.
- Gated bulk low-balance retry to today's orders only: both the bulk sync pass and eligible-order scan now use today's date window.
- Updated Orders UI copy to "Retry today's low balance" and feedback text to explicitly report today's synced/scanned/retried counts.
- No new SQL migration.
- Optimized today's bulk low-balance retry speed: the pre-retry sync step now scopes to today's `failed|pending` orders only and skips fallback auto-reorder side effects on this path.
- This keeps retry behavior focused on today's low-balance submission targets instead of scanning/syncing unrelated statuses.
- No new SQL migration.

2026-08-10
- Changed slot routing from shuffled selection to sequential selection: when multiple slots match, the lowest slot index is now used first.
- Updated Services page guidance text to reflect sequential slot behavior.
- Tightened Services numeric search so digit-only queries match provider service IDs by ID prefix only, preventing unrelated hits from name/rate/min/max text.
- Numeric ID results are now shown in ascending ID order.
- No new SQL migration.
